import { ShowTablesDirective } from './show-tables.directive';

describe('ShowTablesDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowTablesDirective();
    expect(directive).toBeTruthy();
  });
});
