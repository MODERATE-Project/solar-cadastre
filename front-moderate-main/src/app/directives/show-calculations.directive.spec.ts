import { ShowCalculationsDirective } from './show-calculations.directive';

describe('ShowCalculationsDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowCalculationsDirective();
    expect(directive).toBeTruthy();
  });
});
